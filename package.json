{
  "name": "electron-experiment",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "run": "electron .",
    "clean": "rm -rf ./build",
    "dist:linux": "electron-packager . test-app --platform=linux --arch=x64 --out build",
    "dist:windows": "electron-packager . test-app --platform=win32 --arch=x64 --out build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^1.7.10",
    "electron-packager": "^10.1.1"
  }
}
